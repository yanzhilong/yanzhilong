@model IEnumerable<yanzhilong.Domain.ArticleCount>

<ol class="list-unstyled">
   
    @foreach (var item in Model)
    {
        <li>@Html.ActionLink(item.category.Name, "Category", "Article",new { CategoryID = item.category.Id },null)<span>(@item.Count)</span></li>
    }
</ol>